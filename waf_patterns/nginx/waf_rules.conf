# Nginx WAF Rules (Generated by json2nginx.py)

# Include this file in your 'server' or 'location' block.

  if ($waf_exceptions = "high") {
    return 403;
  }
  if ($waf_exceptions = "medium") {
    add_header X-WAF-Blocked "medium-exceptions";
  }
  if ($waf_exceptions = "low") {
     add_header X-WAF-Blocked "low-exceptions";
  }

  if ($waf_attack = "high") {
    return 403;
  }
  if ($waf_attack = "medium") {
    add_header X-WAF-Blocked "medium-attack";
  }
  if ($waf_attack = "low") {
     add_header X-WAF-Blocked "low-attack";
  }

  if ($waf_generic = "high") {
    return 403;
  }
  if ($waf_generic = "medium") {
    add_header X-WAF-Blocked "medium-generic";
  }
  if ($waf_generic = "low") {
     add_header X-WAF-Blocked "low-generic";
  }

  if ($waf_enforcement = "high") {
    return 403;
  }
  if ($waf_enforcement = "medium") {
    add_header X-WAF-Blocked "medium-enforcement";
  }
  if ($waf_enforcement = "low") {
     add_header X-WAF-Blocked "low-enforcement";
  }

  if ($waf_lfi = "high") {
    return 403;
  }
  if ($waf_lfi = "medium") {
    add_header X-WAF-Blocked "medium-lfi";
  }
  if ($waf_lfi = "low") {
     add_header X-WAF-Blocked "low-lfi";
  }

  if ($waf_php = "high") {
    return 403;
  }
  if ($waf_php = "medium") {
    add_header X-WAF-Blocked "medium-php";
  }
  if ($waf_php = "low") {
     add_header X-WAF-Blocked "low-php";
  }

  if ($waf_sqli = "high") {
    return 403;
  }
  if ($waf_sqli = "medium") {
    add_header X-WAF-Blocked "medium-sqli";
  }
  if ($waf_sqli = "low") {
     add_header X-WAF-Blocked "low-sqli";
  }

  if ($waf_xss = "high") {
    return 403;
  }
  if ($waf_xss = "medium") {
    add_header X-WAF-Blocked "medium-xss";
  }
  if ($waf_xss = "low") {
     add_header X-WAF-Blocked "low-xss";
  }

  if ($waf_rfi = "high") {
    return 403;
  }
  if ($waf_rfi = "medium") {
    add_header X-WAF-Blocked "medium-rfi";
  }
  if ($waf_rfi = "low") {
     add_header X-WAF-Blocked "low-rfi";
  }

  if ($waf_fixation = "high") {
    return 403;
  }
  if ($waf_fixation = "medium") {
    add_header X-WAF-Blocked "medium-fixation";
  }
  if ($waf_fixation = "low") {
     add_header X-WAF-Blocked "low-fixation";
  }

  if ($waf_rce = "high") {
    return 403;
  }
  if ($waf_rce = "medium") {
    add_header X-WAF-Blocked "medium-rce";
  }
  if ($waf_rce = "low") {
     add_header X-WAF-Blocked "low-rce";
  }

  if ($waf_java = "high") {
    return 403;
  }
  if ($waf_java = "medium") {
    add_header X-WAF-Blocked "medium-java";
  }
  if ($waf_java = "low") {
     add_header X-WAF-Blocked "low-java";
  }

